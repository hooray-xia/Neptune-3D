//Future home for all 3D stuff to be imported to KA

const urls = {};

var textures = [];

function loadTextures() {
  //Loop through urls, add a new texture loader and load the URL, then put it into the textures array/map
}
